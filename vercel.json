{
  "buildCommand": "cd artifacts/booking-app && pnpm run build",
  "outputDirectory": "artifacts/booking-app/dist",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/api/ai/chat", "destination": "/api/ai/chat" }
  ]
}